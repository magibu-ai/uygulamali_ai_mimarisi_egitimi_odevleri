{
  "name": "melomani",
  "version": "1.0.0",
  "description": "A CD shop assistant that can only speak from its database — tool calling over SQLite, in Node and in the browser.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "demo": "node scripts/demo.mjs",
    "demo:script": "node scripts/demo.mjs --script",
    "demo:db": "node scripts/demo.mjs --db-only",
    "serve": "python3 -m http.server 8000"
  },
  "dependencies": {},
  "devDependencies": {}
}
