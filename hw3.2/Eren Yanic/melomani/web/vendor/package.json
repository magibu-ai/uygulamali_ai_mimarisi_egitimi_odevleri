{
  "_comment": "sql.js ships as a UMD script. The root package.json sets \"type\": \"module\", which would make Node parse this directory's .js as ESM and hand back an empty namespace. This marker scopes it back to CommonJS so tests/browser-adapter.test.mjs can require() it. The browser loads sql-wasm.js via a <script> tag and is unaffected.",
  "type": "commonjs"
}
