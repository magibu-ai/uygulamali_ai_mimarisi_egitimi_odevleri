# Local-only PostgreSQL settings. Copy to db/.env before changing defaults.
POSTGRES_PORT=5432
POSTGRES_ADMIN_PASSWORD=local-admin-only

PLANNING_MIGRATOR_PASSWORD=planning-migrator-local-only
PLANNING_RUNTIME_PASSWORD=change-me
MEMORY_MIGRATOR_PASSWORD=memory-migrator-local-only
MEMORY_RUNTIME_PASSWORD=change-me
